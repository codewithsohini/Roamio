# ==============================================================================
# Roamio Backend — Python Dependencies
# ==============================================================================
# Pin major versions for reproducibility.
# Run: pip install -r requirements.txt

# Web framework
fastapi==0.111.0
uvicorn[standard]==0.29.0

# Email validation (required by pydantic EmailStr)
email-validator==2.3.0

# Form data parsing (required for OAuth2PasswordRequestForm)
python-multipart==0.0.9

# Database
sqlalchemy==2.0.30
alembic==1.13.1
psycopg2-binary==2.9.9

# Data validation and settings
pydantic==2.7.1
pydantic-settings==2.2.1

# Authentication
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1

# HTTP client (used by AI provider layer to call watsonx.ai)
httpx==0.27.0

# Environment variable loading
python-dotenv==1.0.1

# Testing
pytest==8.2.0
pytest-asyncio==0.23.6
httpx==0.27.0
